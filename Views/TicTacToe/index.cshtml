@{
    ViewData["Title"] = "TicTacToe";
}

<link rel="stylesheet" href="~/css/TicTacToeLobby.css">

<h1>TicTacToe</h1>
<h3>Available lobbies:</h3>

<div id="lobby-list-container"> 

    <ul id="lobbies">
        @foreach(var item in (IEnumerable<KeyValuePair<string, int>>)ViewData["lobbies"]!) {
            <li>
                @{
                    if (item.Value < 2) {
                        <a href="/tictactoe/game/@item.Key">
                            <div>
                                <p>@item.Key</p>
                                <p>@item.Value</p>
                            </div>
                        </a>
                        <br>
                    }
                }
            </li>
        }
    </ul>
</div>


<button id="create-button">Create</button>

<button id="refresh-button">Refresh</button>

<script src="~/js/TicTacToeLobby.js"></script>
<script>
    
</script>